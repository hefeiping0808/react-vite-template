# React + TypeScript + Vite

一个 React + TypeScript + Vite 精简模板空项目，能帮你省下10分钟的创建空项目时间。

状态管理用的是 zustand，样式插件用的是 tailwind，路由用的是 react-router-dom。

没有其他乱七八糟占空间的东西，用不到哪些插件自己卸载就行，本身也没几个插件。

npm装不上就用pnpm，node版本是18.20.4。

没了。

